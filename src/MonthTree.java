/*
 * MonthTree class: collection of months. Main data structure of MyCalendar
 */
public class MonthTree {
	/*
	 * Construct an empty MonthTree
	 */
	public MonthTree() {
		
	}
	
	/*
	 * Add an event to the tree
	 * @param e an Event object to be added
	 * @return true if adding success, false otherwise (when there is a 
	 *         conflict or the event is already scheduled)
	 */
	public boolean addEvent(Event e) {
		return true;
	}
}
